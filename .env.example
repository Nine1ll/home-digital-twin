APP_ENV=development
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=
DATABASE_URL=sqlite:///./twin.db
CORS_ORIGINS=
# Optional: Ollama server reachable by the backend (never the user's browser)
OLLAMA_URL=
OLLAMA_MODEL=qwen2.5vl:7b
